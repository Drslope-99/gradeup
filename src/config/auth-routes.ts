const createAccountGroup = ["/login", "/forgot-password", "/reset-password"];

const loginGroup = ["/register", "/verify", "/onboarding"];

export const authRoutes = {
  createAccountGroup,
  loginGroup,
};
